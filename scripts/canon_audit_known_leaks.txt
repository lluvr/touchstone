# Known-leaks fixture for canon_audit.sh self-test.
#
# Every non-comment, non-blank line below MUST be matched by the audit's
# rigid or vault patterns. If a line stops matching, the audit has a
# bug — either the pattern was weakened or a new variant slipped in.
#
# When a new shape is added to PUBLIC_CANON_DISCIPLINE.md §3c, add a
# representative line here AND extend the audit's regex. Both ship
# together.

# ── §3c rigid: operator-side / operator-internal ──
This is operator-side notes and should not appear in public surface.
The operator-internal evaluation rubric is referenced here.

# ── §3c rigid: the operator's [optional adjective] [strategy|...] ──
# Adjectives intervening between "operator's" and the leak noun do not exempt.
# Note: "corpus" / "baseline" / "paper" deliberately NOT in this alternation —
# domain-overloaded in cma/touchstone/lodestone where "operator" means "AI
# practitioner" and "the operator's corpus" is generic. Catch shape-shifted
# leaks like "the operator's research corpus" via the §5a fresh-reader test.
See the operator's strategy memo for context.
The work continues in the operator's research vault on disk.
The work continues in the operator's experimental dev tree.
Compare against the operator's positioning analysis from last quarter.

# ── §3c rigid: bare operator [paper|study|playbook|doctrine|memo|brief] ──
# Limited to artifact-shape words. Words like "operator methodology",
# "operator practice", "operator framework", "operator positioning" are
# too domain-shaped to flag mechanically (lodestone uses "operator" as
# vocabulary for AI-practitioner; the overlap is incidental).
Reading: operator playbook for the team.
This is operator memo content.
See operator brief for findings.
The operator paper lives elsewhere.
The operator study cited prior work.
Operator doctrine for AI-coupled work.

# ── §3c rigid: private [operator|...] ──
Mirror from the private upstream when stable.
Forked into the private fork for auditing.

# ── §3c rigid: meta-reference parentheticals ──
This is calibrated upstream (see private).
The decision lives elsewhere (internal reference).
Threshold tuning applies (operator-side reference).
See the protocol (see operator-side studies).

# ── §3c rigid: internal version / unredacted / the full X ──
The internal version of this rubric is more detailed.
See unredacted memo for the cited number.
The full methodology lives outside this repo.
Refer to the full ground-truth corpus for verification.
The full specification ships separately.

# ── §3c rigid: extracted-from / canonical-lives ──
Extracted from the operator's research vault.
The canonical research lives in the upstream tree.
The canonical methodology lives separately.

# ── §3c rigid: strategic-vocabulary bigrams ──
Our trust moat comes from authorship.
Data moat is the durable advantage.
The compounding claim survives this round.
This is named-authorship moat at work.
Methodology-as-moat framing.

# ── §3c rigid: empire vocabulary ──
This is empire-grade infrastructure.
Empire-wide rollout planned.
The Clarethium-empire shape favors composition.
The project's empire is built on canon.

# ── §3c rigid: construct-honesty discipline ──
Construct-honesty discipline applies at every layer.
We follow the construct honesty discipline strictly.

# ── §3c rigid: Powerhouse.localdomain hostname ──
Author: lluvr <llucic@Powerhouse.localdomain>

# ── §3c vault-as-architecture ──
Library is vault-faithful: regex preserved as-is.
Boundaries are vault-validated against EXP-095 data.
Vault behaviour: text before first ## heading is excluded.
The vault's precision threshold for entities is total < 5.
This implementation pins vault notes for posterity.
Outputs live in JSON files in the vault EXP-095 directory.
This baseline comes from the vault implementation.
