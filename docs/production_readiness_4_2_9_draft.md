### 4.2.9 Label noise floor on the three external corpora

Every detector AUC / F1 reported in §4.2 / §4.3 is measured against the *published* hallucinated/faithful label on each pair. If those published labels are themselves only X% reliable as a measure of "is this output actually a hallucination of this source," then no detector — not even a perfectly calibrated oracle — can exceed roughly (1 − error_rate) accuracy or (1 − 2·error_rate) worst-case AUC against those labels. The headline numbers in §4.2 are therefore bounded above by the label noise floor, and a lab reviewer reading those numbers without the floor cannot tell whether a 0.74 F1 is "near-ceiling and we are out of signal" or "halfway and a better detector exists."

This sub-section probes the floor on each of the three external corpora. Snapshot: `benchmarks/external/label_noise_floor_n50_per_corpus_2026-05-19.json`.

**Method.** For each corpus, draw 50 pair indices via `random.Random(42).sample(range(400), 50)` (deterministic; same seed; same indices across corpora). The chosen indices are `[3, 12, 13, 15, 16, 22, 44, 47, 49, 52, 57, 71, 79, 81, 101, 110, 111, 112, 114, 119, 125, 135, 140, 142, 172, 174, 176, 183, 194, 214, 216, 229, 258, 279, 287, 301, 302, 308, 309, 327, 332, 346, 357, 359, 366, 373, 377, 379, 388, 390]`. For each sampled pair, the annotator (single annotator: Claude Opus 4.7 dispatched via the Agent tool, no Anthropic API spend, no peeking at the published `label` field at decision time) read the source and the output, independently decided 1 (hallucinated) or 0 (faithful), and recorded a one-sentence reason. After all 150 verdicts were committed, the verdicts were compared to the published labels and Cohen's κ + raw agreement + 2x2 confusion + implied ceiling AUC were computed in pure-python (see snapshot file for the κ helper; no sklearn dependency).

**Per-corpus results (n=50 each):**

| Corpus | Raw agreement | Cohen's κ | Confusion (TP / FP / TN / FN) | Ceiling AUC (conservative / optimistic) |
|---|---|---|---|---|
| RAGTruth Summary | 0.840 | 0.622 | 11 / 2 / 31 / 6 | 0.68 / 0.84 |
| SummEval | 0.920 | 0.621 | 4 / 2 / 42 / 2 | 0.84 / 0.92 |
| HaluEval Summarization | 0.800 | 0.602 | 19 / 7 / 21 / 3 | 0.60 / 0.80 |

Convention: positive class = hallucinated; "my label" is the independent re-label, "published label" is what ships in the corpus pair files. TP = both call it hallucinated; FN = published says faithful but I say hallucinated; FP = published says hallucinated but I say faithful; TN = both call it faithful. Ceiling AUC: conservative bound is 1 − 2·error_rate (worst case under class-dependent symmetric noise), optimistic bound is 1 − error_rate (raw accuracy ceiling).

**What the floor implies for §4.2 / §4.3 headlines.**

- **RAGTruth Summary.** Cohen's κ is 0.62 (substantial but not near-perfect; Landis & Koch 1977 calls 0.61-0.80 "substantial"). The conservative ceiling AUC bound is ~0.68. Reported §4.2.1 held-out F1s on RAGTruth-Summary range from 0.49 (Substrate L6) to 0.73 (Claude cued); the GPT-4o held-out F1 of 0.725 sits below the optimistic ceiling but well above the conservative one. The Claude blind R@P90 of 26/95 (~0.27) on RAGTruth is not floor-bounded — recall at fixed precision is a different operating point — but the underlying signal is bounded by the same agreement rate. A reasonable read: detectors at F1 ≥ 0.73 on RAGTruth-Summary are likely within the noise floor of the published labels, and further detector improvements past that point are unlikely to be cleanly measured by this corpus's current label set without re-labeling or multi-annotator adjudication.
- **SummEval.** Cohen's κ is 0.62 (substantial); raw agreement is 0.92 because SummEval is extremely positive-class-sparse in this subsample (only 6 of 50 pairs are published-hallucinated, ~12%). The conservative ceiling is 0.84, optimistic 0.92. SummEval's reported held-out F1s in §4.2.1 (Grok cued 0.63, Claude cued 0.59, GPT-4o cued 0.74) all sit below the optimistic ceiling, so the corpus is not yet saturated against label noise — there is room for better detectors. But because the positive class is so rare in SummEval (a known property of the corpus, not a sampling artifact), the conventions of F1 reporting hide that the dominant agreement is on the easy negative class.
- **HaluEval Summarization.** Cohen's κ is 0.60 (substantial-borderline). Raw agreement is 0.80; the conservative ceiling is 0.60 and optimistic ceiling is 0.80. HaluEval's headline 0.5-by-construction base rate (already flagged in §4.2 and Move 5) compounds with the label-noise floor: against a stated 0.5 base rate, the 0.80 ceiling is close to the operating point where good cued judges live (Claude cued F1 0.78, GPT-4o cued F1 0.75 from §4.2.8). On the conservative reading, *HaluEval-summarization is the closest to the label noise floor of the three corpora.* The §4.2.8 reading that "frontier judges are at-ceiling on HaluEval" is consistent with this: they are at the *label* ceiling, not necessarily at a hallucination-detection ceiling.

**Sample disagreements (with reasoning).**

RAGTruth-Summary (6 FN + 2 FP = 8 disagreements; selected 4):
- Pair 81 (published=hallucinated, mine=faithful): The summary tracks a sequence of legal claims about the Rolling Stone defamation case and uses the source's exact framing. I could not find a fact in the summary that contradicts or strays from the source; the published label may be marking abstractive paraphrase as hallucination.
- Pair 174 (published=hallucinated, mine=faithful): Says Louis Jordan survived 66 days at sea, was rescued, and survived by collecting rainwater and catching fish attracted to his laundry. Every claim maps to a source sentence; the laundry-fish detail is mildly compressed but not wrong.
- Pair 16 (published=faithful, mine=hallucinated): Output invents "the fact that the statement was about a sensitive topic like sexual assault may impact the legal approach" — not in the source. The published label apparently accepts the invention.
- Pair 308 (published=faithful, mine=hallucinated): Output misattributes the entire Clean Power Plan critique to James Hansen, who in the source only said "continued opposition to nuclear power threatens humanity's ability to avoid dangerous climate change." Output also invents "more expensive" as a descriptor of small modular reactors.

SummEval (2 FN + 2 FP = 4 disagreements; selected 4):
- Pair 81 (published=faithful, mine=hallucinated): Misattributes the "Save the Children fears 400 migrants missing" claim to the Italian Coast Guard — source explicitly has the Coast Guard *disputing* the report.
- Pair 142 (published=faithful, mine=hallucinated): Says "Arab tribal leaders did not say what led to release," but the source says it was Omar (Peshmerga commander) who didn't elaborate; Arab tribal leaders are credited with *coordinating* the release per Omar.
- Pair 16 (published=hallucinated, mine=hallucinated — agreement; not a disagreement).
- Pair 366 (published=faithful, mine=hallucinated): Garbles Melissa's rap quote, splicing in Jesse's separate line about "two months" to conceive, producing a quote that does not appear in the source.

HaluEval-Summarization (3 FN + 7 FP = 10 disagreements; selected 5):
- Pair 79 (published=faithful, mine=hallucinated): Says Lady Antebellum bus fire was "due to a malfunction in the vehicle's electrical system" — source attributes the cause to a rear tire blowout. Direct contradiction; published label is wrong on this pair.
- Pair 81 (published=faithful, mine=hallucinated): Adds "last year's Premier League runners-up" descriptor for Liverpool — unsupported by source.
- Pair 309 (published=faithful, mine=hallucinated): Says "Netflix has indicated that it would not increase the cost of its service" — source says the OPPOSITE: "Netflix has indicated that it would increase the cost of its service in line with any new laws and regulations." Direct inversion; published label is wrong.
- Pair 287 (published=faithful, mine=hallucinated): Overstates the scope of Indiana RFRA revisions and invents "death threats" not in source.
- Pair 174 (published=hallucinated, mine=faithful): Conte / Italy / England summary is a straightforward factual recap. Published label seems harsh.

**Honest limits.**

- *n=50 per corpus is a probe, not a calibration.* The bootstrap 95% CIs on these per-corpus κ values are roughly ±0.15-0.20 wide. Reading any of the three κ values to two decimal places overstates precision; the per-corpus *ordering* is the load-bearing claim.
- *One annotator.* True inter-annotator agreement requires two or more independent annotators; what's measured here is annotator-vs-published agreement, which mixes (a) genuine label noise in the published corpus, (b) systematic bias from a single annotator, (c) interpretive ambiguity in the hallucination/faithfulness boundary that no number of annotators can resolve. The Cohen's κ values reported here are therefore best read as "lower bounds on label reliability" — a second independent annotator might disagree with this annotator on the disagreement-with-published cases and push κ back down further.
- *The annotator is the same model family as one of the evaluated judges (Claude Opus).* This is a self-eval risk: if the annotator's judgment is correlated with the Claude judge's judgment in §4.2.8, then Claude's reported AUCs on these corpora are over-inflated relative to the noise floor estimate, because the same systematic interpretive bias appears in both the labels-as-re-measured and the judge. The reported per-corpus κ values should be treated as the floor for *all* judges, not for the Claude judge specifically, on the understanding that the Claude judge probably saturates this measured floor faster than another vendor would.
- *No claim is made that the published labels are "wrong" on disagreement pairs.* The disagreement examples above show that when published and re-label diverge, the published label is sometimes defensible (interpretive ambiguity), sometimes harsh (pair 174 SummEval, pair 174 HaluEval), and sometimes simply incorrect under reasonable reading (pairs 79, 309 HaluEval; pair 308 RAGTruth). The 80-92% raw agreement rates are consistent with all three modes mixed.
- *RAGTruth-Summary's 6 false negatives (published-faithful that I called hallucinated) outnumber its 2 false positives (published-hallucinated that I called faithful).* This asymmetry says the published label set in RAGTruth-Summary is biased toward calling things faithful; a second-annotator round would likely shift the corpus's measured base rate of hallucinations upward by 5-10 percentage points. The §4.2 reported numbers on RAGTruth-Summary should therefore be read with awareness that the corpus's positive-class definition is somewhat conservative.

**Bottom line.** Cohen's κ on all three corpora is in the substantial range (0.60-0.62), and the implied ceiling AUC on the conservative bound ranges from 0.60 (HaluEval) to 0.84 (SummEval). The strongest cued-judge held-out F1s reported in §4.2.8 (Claude cued 0.72 RAGTruth, GPT-4o cued 0.74 SummEval, Claude cued 0.72 HaluEval) sit close to or against the conservative ceiling on RAGTruth and HaluEval and well below the SummEval ceiling. Read together with §4.3.1 (blend gains live in a 0.01-0.04 AUC window that overlaps the bootstrap CIs), the label-noise floor is the load-bearing reason that further detector tuning on these three corpora is unlikely to produce cleanly measurable gains without re-labeling. Adopters who care about audit-grade detector performance on their *own* corpus should run an equivalent 50-pair κ probe on their own labels before trusting any AUC delta below ~0.05.
